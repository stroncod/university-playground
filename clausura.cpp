#include "clausura.h"
#include <vector>
#include <stack>
#include <string>

void clausura(std::string estado,std::stack pila,std::vector <std::vector <std::string> > v ){
	for (const auto &i : v) {
		
        	for (const auto &j: i){
            	

        	}
      }
    


}